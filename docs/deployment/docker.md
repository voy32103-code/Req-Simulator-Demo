# Docker

Docker is not required for the static prototype.

Future production setup should include:

- Frontend Dockerfile if self-hosting the built frontend
- Backend Dockerfile
- Docker Compose for local PostgreSQL
- Environment variable injection for AI provider keys
