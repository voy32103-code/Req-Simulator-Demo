# Routing

The static prototype uses in-page state instead of URL routing.

Future React or Next.js routing should include:

- `/login`
- `/dashboard`
- `/scenarios`
- `/scenarios/:id`
- `/sessions/:id`
- `/sessions/:id/submit`
- `/sessions/:id/feedback`
- `/instructor`
