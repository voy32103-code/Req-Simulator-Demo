# Frontend

ReactJS frontend for Req Simulator.

## Run

```text
npm install
npm run dev
```

The frontend expects the ASP.NET backend at:

```text
http://localhost:5088/api
```

Set `VITE_API_BASE_URL` if the backend uses another URL.
